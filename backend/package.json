{
  "name": "livraria-api",
  "version": "1.0.0",
  "description": "API para catálogo de livros",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "migrate": "npx prisma migrate dev --name init",
    "build": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "prisma": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}